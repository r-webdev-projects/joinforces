<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
	<% @users.each do |user| %>
		<li>
			<%= image_tag(user.gravatar_url('50')) %>
			<%= link_to user.first_name, user %>
		</li>
	<% end %>
</ul>