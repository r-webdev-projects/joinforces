<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul>
	<% @users.each do |user| %>
		<li>
			<%= image_tag(user.gravatar_url('50')) %>
			<%= link_to user.username, user %>
		</li>
	<% end %>
</ul>

<%= will_paginate @posts %>